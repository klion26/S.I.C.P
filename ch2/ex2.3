(define (make-rect left-top right-bottom)
  (cons left-top right-bottom))
(define (left-top-rect r)
  (car r))
(define (right-bottom-rect r)
  (cdr r))
(define (perimeter r)
  (* 2 (+ (abs (- (x-point (left-top-rect r)) (x-point (right-bottom-rect r))))
          (abs (- (y-point (left-top-rect r)) (y-point (right-bottom-rect r)))))))
(define (area r)
  (* (abs (- (x-point (left-top-rect r)) (x-point (right-bottom-rect r))))
     (abs (- (y-point (left-top-rect r)) (y-point (right-bottom-rect r))))))
(define ot (make-rect one-one two-two)) 
